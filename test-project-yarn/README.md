# Test Project (Yarn)

This fixture project uses Yarn as its package manager. It exists so `pipery-npm-ci` can verify that the action correctly detects `yarn.lock` and invokes Yarn commands during `pipery-actions test`.
