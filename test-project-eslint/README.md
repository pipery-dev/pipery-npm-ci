# Test Project (ESLint)

This fixture project has ESLint configured via `.eslintrc.json`. It exists so `pipery-npm-ci` can verify that the lint step detects and runs ESLint when a config file is present during `pipery-actions test`.
